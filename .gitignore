node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.local

# מצב מקומי של כלי Netlify (נוצר ע"י netlify serve / deploy)
.netlify/

# הגדרות ומיומנויות מקומיות של Claude Code / העורך — לא חלק מהפרויקט
.claude/
.vscode/
.idea/

# מסמכים פנימיים — לא לריפו הציבורי
*.pdf
docs/

# נוצר ע"י סביבת ה-Edge Functions של Netlify
deno.lock
